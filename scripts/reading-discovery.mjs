const escape = value => String(value ?? '').replaceAll('&', '&amp;').replaceAll('<', '&lt;').replaceAll('>', '&gt;').replaceAll('"', '&quot;')

export function directoryTools(language) {
  const zh = language === 'zh'
  return `<form class="directory-tools" role="search" aria-label="${zh ? '筛选本目录' : 'Filter this directory'}" hidden><label>${zh ? '查找条目' : 'Find an entry'}<input type="search" name="q" maxlength="240" placeholder="${zh ? '中文、English、学名或编号' : 'Name, 中文, scientific name or ID'}" aria-describedby="directory-help" autocomplete="off"></label><label>${zh ? '排列顺序' : 'Sort entries'}<select name="sort"><option value="source">${zh ? '原始顺序' : 'Source order'}</option><option value="name">${zh ? '名称升序' : 'Name, ascending'}</option><option value="name-desc">${zh ? '名称降序' : 'Name, descending'}</option></select></label><button type="button">${zh ? '清除筛选' : 'Clear filters'}</button><p id="directory-help">${zh ? '空格分隔多个关键词；可复制地址栏分享筛选结果。' : 'Separate keywords with spaces. Copy the page address to share your results.'}</p><output aria-live="polite" aria-atomic="true"></output></form><p class="notice" data-directory-empty hidden>${zh ? '没有匹配条目。试试更短的关键词，或清除筛选。' : 'No entries match. Try a shorter keyword or clear the filters.'}</p>`
}

export function readingHome({ language, basePath, title, description, collections, stories, periods, mapFrame }) {
  const zh = language === 'zh'
  const prefix = `${basePath}/${zh ? 'zh/' : ''}`
  // The caller orders featured stories from the shared edition selection.
  const chosen = stories.filter(story => story.featured).slice(0, 3)
  if (chosen.length !== 3) throw new Error('Three featured reading trails are required.')
  const trail = chosen.map((story, index) => `<li><span class="trail-number" aria-hidden="true">0${index + 1}</span><div><a href="${prefix}stories/${escape(story.id)}/"><h3>${escape(zh ? story.titleZh : story.title)}</h3></a><p>${story.durationMinutes} ${zh ? '分钟' : 'min'} · ${story.steps.length} ${zh ? '个证据步骤' : 'evidence steps'}</p></div><span aria-hidden="true">↗</span></li>`).join('')
  const times = [...periods].sort((a, b) => b.eag - a.eag).map(period => {
    if (!/^#[\da-f]{6}$/i.test(period.col)) throw new Error(`Invalid period color: ${period.oid}`)
    return `<li style="--period-color:${period.col}"><a href="${prefix}intervals/${escape(period.oid.split(':').at(-1))}/"><strong>${escape(zh ? period.namZh : period.nam)}</strong><span>${period.eag}–${period.lag} Ma</span></a></li>`
  }).join('')
  return `<div class="reading-intro"><div><h1>${escape(title)}</h1><p class="dek">${escape(description)}</p><div class="actions"><a class="button" href="${prefix}taxa/">${zh ? '查找一个类群' : 'Find a taxon'}</a><a class="button secondary" href="#reading-trails">${zh ? '从一篇故事开始' : 'Start with a story'}</a></div></div><figure class="reading-preview"><a href="${prefix}${mapFrame.path}"><img src="${basePath}/${mapFrame.file}" width="1080" height="540" loading="lazy" alt="${zh ? '寒武纪' : 'Cambrian'} · ${mapFrame.ageMa} Ma · CAO2024 ${zh ? '模型海岸线和板块边界' : 'modelled coastlines and plate boundaries'}"></a><figcaption><strong>${zh ? '让证据回到当时的地球' : 'Place the evidence on its Earth'}</strong><span>${mapFrame.ageMa} Ma · Cao et al. (2024) · CC-BY-4.0</span><span>${zh ? '模型重建，非直接观测；打开地图查看来源和局限。' : 'A model reconstruction, not direct observation. Open the map for sources and limits.'}</span></figcaption></figure></div><section id="reading-trails"><h2>${zh ? '沿着证据读下去' : 'Follow an evidence trail'}</h2><p>${zh ? '从具名记录出发，逐步查看主张、参考文献与解释边界。' : 'Start with named records, then follow claims, references and the limits of each interpretation.'}</p><ol class="reading-trails">${trail}</ol><a href="${prefix}stories/">${zh ? '浏览全部故事' : 'Browse all stories'} →</a></section><section><h2>${zh ? '按地质年代进入' : 'Enter through geological time'}</h2><p>${zh ? '从古老到近期排列；Ma 表示距今百万年，条目宽度不代表时长。年代边界与不确定性见各条目。' : 'Ordered from older to younger. Ma means millions of years ago; widths do not encode duration. Each entry retains boundary uncertainty and sources.'}</p><ol class="reading-time">${times}</ol></section><section><h2>${zh ? '浏览完整目录' : 'Explore the collections'}</h2><div class="edition-grid">${collections.map(([kind, en, cn, count]) => `<a href="${prefix}${kind}/"><strong>${escape(zh ? cn : en)}</strong><span>${count.toLocaleString(zh ? 'zh-CN' : 'en')} ${zh ? '条目' : 'entries'}</span></a>`).join('')}</div></section><aside class="notice"><strong>${zh ? '阅读时保留证据边界' : 'Keep the evidence boundary in view'}</strong><p>${zh ? '原有引用、不确定性与审阅状态保持可见；自动化校验不等于科学评审。' : 'Citations, uncertainty and review status remain visible; automated validation is not scientific review.'}</p><a href="${prefix}methods/">${zh ? '方法与证据边界' : 'Methods and evidence boundaries'}</a> · <a href="${prefix}apps/">${zh ? '获取完整应用' : 'Get the full applications'}</a></aside>`
}

export const readingDiscoveryCss = `
.directory-context{font-size:14px;color:var(--muted)}.directory-entries{margin-top:20px;padding-top:12px}.directory-tools{display:grid;grid-template-columns:minmax(0,1fr) minmax(160px,220px) auto;gap:14px;align-items:end;padding:22px 0;border-block:1px solid var(--line);margin:24px 0}.directory-tools[hidden],[data-directory-empty][hidden],.directory li[hidden]{display:none}.directory-tools label{display:grid;gap:8px;font-size:14px}.directory-tools input,.directory-tools select,.directory-tools button{min-height:44px;min-width:0;padding:10px 12px;border:1px solid #547575;background:#11272b;color:var(--text);font:inherit;border-radius:3px}.directory-tools input{width:100%}.directory-tools button{cursor:pointer}.directory-tools p{grid-column:1/-1;margin:0;color:var(--muted);font-size:14px}.directory-tools output{grid-column:1/-1;color:var(--accent);font-variant-numeric:tabular-nums}.directory{list-style:none;padding:0}.directory li a{display:block;min-height:44px}.directory li a:hover,.edition-grid a:hover{background:#11272b}.reading-intro{display:grid;grid-template-columns:1.1fr 1fr;align-items:center;gap:36px}.reading-intro h1{font-size:clamp(38px,5vw,66px);line-height:1.08}.reading-preview{margin:0}.reading-preview img{display:block;width:100%;height:auto;border:1px solid #547575}.reading-preview figcaption{padding:14px 0;border-bottom:1px solid var(--line)}.reading-preview figcaption strong,.reading-preview figcaption span{display:block}.reading-preview figcaption span{margin-top:6px;font-size:12px;color:var(--muted)}.reading-trails{list-style:none;padding:0}.reading-trails li{display:grid;grid-template-columns:40px minmax(0,1fr) 24px;gap:18px;align-items:center;padding:20px 0;border-top:1px solid var(--line)}.reading-trails h3{margin:0;font-size:22px}.reading-trails p{margin:8px 0 0;color:var(--muted);font-size:14px}.reading-trails a{text-decoration:none}.reading-trails a:hover{text-decoration:underline}.trail-number{color:var(--accent);font-variant-numeric:tabular-nums}.reading-time{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));list-style:none;padding:0;gap:10px}.reading-time a{display:block;min-height:72px;padding:12px;border-top:4px solid var(--period-color);background:#11272b;text-decoration:none}.reading-time strong,.reading-time span{display:block}.reading-time span{margin-top:6px;font-size:12px;color:var(--muted);font-variant-numeric:tabular-nums}#reading-trails{scroll-margin-top:24px}
@media(max-width:750px){.reading-intro{grid-template-columns:1fr;gap:24px}.reading-time{grid-template-columns:repeat(2,minmax(0,1fr))}.directory-tools{grid-template-columns:minmax(0,1fr) auto}.directory-tools label:first-child{grid-column:1/-1}.reading-trails h3{font-size:20px}.reading-trails li{gap:10px;grid-template-columns:30px minmax(0,1fr) 16px}}
`
