import { rc77EchinodermsZh } from './rc77EchinodermsZh'

/** Exact keys only: derived from the translation map to prevent marker-based false positives. */
export const rc77EchinodermsZhKeys = new Set<string>(Object.keys(rc77EchinodermsZh))
