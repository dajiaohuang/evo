import { rc77PlantsZh } from './rc77PlantsZh'

/** Exact allowlist derived from the translation map; no marker-based matching. */
export const rc77PlantsZhKeys = new Set<string>(Object.keys(rc77PlantsZh))
